version: "2"
sql:
  - engine: "sqlite"
    queries: "repo/sqlite/queries"
    schema: "repo/sqlite/migrations"
    gen:
      go:
        package: "sqlite"
        out: "repo/sqlite/db"
        overrides:
          - db_type: "DATETIME"
            go_type: "time.Time"
