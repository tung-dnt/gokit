version: "2"
sql:
  - engine: "sqlite"
    queries: "pkg/sqlite/queries"
    schema: "pkg/sqlite/migrations"
    gen:
      go:
        package: "sqlitedb"
        out: "pkg/sqlite/db"
        overrides:
          - db_type: "DATETIME"
            go_type: "time.Time"
