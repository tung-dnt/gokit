version: "2"
sql:
  - engine: "sqlite"
    queries: "infra/sqlite/queries"
    schema: "infra/sqlite/migrations"
    gen:
      go:
        package: "sqlitedb"
        out: "infra/sqlite/db"
        overrides:
          - db_type: "DATETIME"
            go_type: "time.Time"
